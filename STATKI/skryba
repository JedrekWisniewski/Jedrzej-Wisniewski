import subprocess
import sys


plik=open('jakisplik.txt','w')# tak sie bedzie nazywal  plik txt


def output():
    subprocess.Popen('a.py', stdout=plik, shell=True)# to co printuje w shellu a.py bedzie zapisane  w pliku txt
output()
